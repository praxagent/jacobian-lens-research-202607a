# ADJUDICATION — Pro plan review (gpt-5.6-sol, 2026-07-14)

Review: `out/plan_review/gpt-5.6-sol-pro_run2/review.md` (10 blocking B01–B10, 7 important
I01–I07). Adjudication per integrity §7A: one row per finding, disposition + the concrete
plan/code change. The reviewer never edits the plan; every accepted blocker is resolved in
PROTOCOL v2 and re-validated before freeze. **No unresolved blocker or needs-human remains
open at freeze.**

Process note: this review ran at `--reasoning-effort high` and cost ~$1.83 vs a $1.75
authorization (overrun ~$0.08; run1 timed out locally and may also have billed ~$1).
Reported to TJ; no further paid model call without approval. A second paid review is NOT
sought — findings are accepted and applied, not re-litigated (§7A: a second call needs a
materially redesigned plan *and* approval).

| ID | Severity | Decision | Resolution in PROTOCOL v2 / code |
|---|---|---|---|
| B01 | Blocking | accepted-modified | Reframe as a **reader benchmark on labeled entity spans**, not a replication of the paper's Gemma-3 number. Gold-label arms: Llama-3.1-8B, Llama-3.3-70B, gemma-2-9b (public labels). Gemma-3-12B becomes an **own-completions + cheaper-grader labeled arm** (TJ, 2026-07-15), stated as *agreement with our grader's rubric, not ground truth*. No comparison to the cited .88/.94 as "reproduction". |
| B02 | Blocking | accepted | Drop "calibrated"; primary construct is **discrimination (AUROC)**. Add a separate, frozen **calibration** sub-analysis for the probe only (logistic map fit on validation; test Brier + log-loss + ECE with frozen bins), never calling a raw lens/SAE score a probability. |
| B03 | Blocking | accepted (code fixed) | `acts.cache_spans(scoring="pre_token")` scores each entity token from the **preceding causal residual**; same states to every reader; teacher-forced/observational, no causal claim. Freeze the token-alignment table + fixtures (B08). Committed f8e2e2d. |
| B04 | Blocking | accepted | Add a **per-arm manifest** (`manifests/<arm>.json`): model+tokenizer revision, chat-template hash, SAE repo revision/file/width/L0/layer/hook/normalization/conventions, probe source commit+full architecture+optimizer+aggregation, logit-lens final-norm+unembed convention, J-lens source/target hooks+matrix hash+fit config, exact span→token aggregation. + fixture tests (B08). |
| B05 | Blocking | accepted | SAE reader relabeled **label-selected sparse reader** (selection uses dev labels). Reader taxonomy: **label-free** = logit lens, J-lens; **label-selected** = SAE coordinate (+ probe = supervised). Drop "capacity-matched", "which object carries the signal", "needs supervision". Claim narrows to "reader A out/under-discriminates reader B on held-out spans". |
| B06 | Blocking | accepted | Replace CI-overlap with **paired completion-clustered bootstrap of reader-minus-probe AUROC differences**; frozen **equivalence margin** (paired 95% CI ⊂ [−0.05,+0.05] ⇒ equivalent; noninferior if lower bound > −0.05). One **primary model** (Llama-3.1-8B) + primary family (probe AUROC + 3 paired reader−probe contrasts); **Holm** across the family; **design-stage precision calc** from cluster/class counts; report completion/span/class/cluster counts; AUROC is **span-weighted**, uncertainty completion-clustered. |
| B07 | Blocking | accepted | Fix model/arm counts everywhere. Gold-label four-reader arms = {Llama-3.1-8B, Llama-3.3-70B, gemma-2-9b}; Gemma-3-12B = own-graded labeled arm. J-lens defined for **every** arm that uses it (fit for 8B/12B/9b; reuse+pin the campaign 70B lens); Gemma-3 spans come from **our generated completions** with frozen generation params/seeds/prompt source, and the grader's entity extraction (I05 caveat folded in — labeled, not agreement-only). |
| B08 | Blocking | accepted | Freeze a **control-gate suite** (`fixtures/`): alignment fixtures (known text→token indices→causal score positions); **native-logit positive control** (output-head lens reproduces the model's native token NLL within tolerance); **J-lens identity control** (identity transport == logit lens); **SAE compatibility control** (dims + encode/decode fixture + firing/reconstruction diagnostics); **leakage negative control** (shuffled-label probe at chance on held-out); **procedure-matched SAE null** (random/permuted latent runs the *same* candidate-count + dev-selection as the label-selected SAE); mechanical dev-only **sign rule**. A failed gate ⇒ logged outcome-masked implementation amendment + rerun affected arms, not a report/no-report choice. |
| B09 | Blocking | accepted | Freeze an **execution manifest** + **two-stage pipeline**: stage-1 score generation **cannot read test labels**; stage-2 locked evaluator joins scores→labels by frozen IDs. Resolve the train/val contradiction: **train** = parameter fitting; **validation** = all model/epoch/sign/latent/calibration/threshold choices; **test** = one final eval. Duplicate-completion-ID + cross-split leakage checks; `annotations`/`canary`/verification fields barred from reader inputs; frozen handling of truncation/tokenizer-mismatch/NaN/OOM/classless/retry/hardware-fallback; fixed seeds (train, bootstrap, controls, generation, J-fit); reruns supplement not replace. bf16 non-determinism acknowledged; report tolerances, don't claim bitwise determinism. |
| B10 | Blocking | accepted | Add the **cost/storage table by arm+stage** (token counts, GPU type, measured-throughput assumption, expected+max hours, $/hr, J-fit cost, extraction cost, receipt/storage volume, total). Seek **one upfront approval for the ENTIRE frozen arm set** (not outcome-contingent per-model). Technical stop rules only (hard spend, wall-time, no-progress) + mandatory reporting of any unrun registered arm. Pin every model/dataset/SAE/lens revision; archive the J-lens or ship a deterministic **refit recipe**; store **per-span sufficient statistics**, not raw 70B activations. |
| I01 | Important | accepted | Null language: "did **not detect** discrimination above chance at the registered precision (estimate, CI)"; reserve "practically no signal" for a passed equivalence test around 0.5. |
| I02 | Important | accepted | Rename "output-head layer" → **native output-token surprisal**; it is the model's native next-token likelihood baseline AND the B08 positive control, not another internal lens. |
| I03 | Important | accepted-modified | Make leave-one-domain **descriptive** (macro-average of per-domain test AUROCs, min class-count rule, undefined domains dropped) — not confirmatory. |
| I04 | Important | accepted | Pin the heuristic baseline fully: frequency corpus+revision, normalization, tokenizer, frequency definition, OOV smoothing, feature transforms, regularization, fit split (dev only), score direction; fit on the same dev/test IDs as the neural readers. |
| I05 | Important | accepted | Superseded for Gemma-3 by the cheaper-grader labeled arm (B07); any residual score-agreement analysis is labeled "**score agreement**" only, includes the length/freq baseline, and never supports the detection claim. |
| I06 | Important | accepted | Train the probe on **3 fixed seeds**; primary = mean of independently trained probes (never best-seed); CI notes it now includes optimization variability. |
| I07 | Important | accepted | Pin the J-lens fit corpus revision+IDs, sampling seed, preprocessing, context length, objective, regularization, token count, source/target hooks, held-out fit diagnostics, matrix hash; **state LongFact prompts/conversations are excluded** from the fit corpus. |

**What the review said to KEEP (do not regress):** mental-state/RL claim restrictions; completion (not entity) as the independent unit; a genuine held-out test split; same-spans reader scoring; no headline layer selection; retained raw per-span receipts.

**Freeze blocked until:** PROTOCOL v2 resolves every B-row, the fixture/gate suite (B08) and two-stage pipeline (B09) are implemented and green on CPU, the per-arm manifests (B04) and cost table (B10) are filled with pinned revisions, and TJ signs off on the frozen plan + the whole-arm-set budget.
